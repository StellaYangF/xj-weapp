<view 
  class='xj-tabs'
>
  <text
    wx:for='{{list}}'
    wx:key='{{index}}'
    class='xj-tab {{ activated == index ? "active": ""}} custom-class'
    data-index='{{index}}'
    bindtap='toggleTab'
  >{{item.title}}</text>
</view>